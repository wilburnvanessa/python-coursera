def count_vowels(word)
    """(str) -> int
    """

    return 

def count_consonants(word)
    """(str) -> int
    """

    return count_vowels(word) + count_consonants(word)


def get_capital(country)
    '''(str) -> str
    Return the name of the capital for the parameter country.
    '''

def longer(word1,word2)
    '''(str) -> str
    Return the longer string of word1 and word2.
    '''

    return get_capital(country,country)


    

    longer(get_capital(country1), get_capital(country2))
    
